Replace the Employee Setup page with inline designation creation in the employee form (frontend only — no API/backend changes).

## 1. Inline designation creation in the employee form

**File: `apps/web/src/components/employee-form.tsx`** (designation field, lines 171–201)

- Wrap the designation `SelectTrigger` and a new small `PlusIcon` button in a `flex items-end gap-2` row (pattern already used by the custom-field manager at lines 320–341). The button uses the existing `Button` with `size="icon-sm"`, `variant="outline"`, `aria-label="Add designation"`, disabled while loading/submitting.
- Clicking it opens the existing `Dialog` primitive from `@tds-nivaran/ui/components/dialog`: title "Add Designation", a `Field`/`FieldLabel`/`Input` for the name, and Create/Cancel buttons — same primitives and styles as the old page's create form (`institution-settings._index.tsx` lines 155–177).
- Empty-state `FieldDescription` copy (lines 195–199) changes from "Create a designation in Employee Setup…" to point at the new + button (e.g. "No designations yet — use the + button to add one."). The submit-disabled-when-no-designations logic (line 394) stays, since the inline flow lets a user create one mid-form.

**File: `apps/web/src/hooks/use-employee-record-editor.ts`**

- Add a `createDesignation` mutation using `trpc.employeeSettings.createDesignation.mutationOptions` (the procedure already exists in `packages/api/src/routers/employee-settings.ts` line 31; no backend change).
- On success, invalidate `employeeSettings.getFormConfig`, `employees.getCreateForm`, `employees.getEditForm` (the exact `invalidateDesignationQueries()` set from the old page) and expose the new designation id so the form can auto-select it via `onFieldChange("designationId", id)`.
- Auto-select: `createDesignation` in `packages/api/src/modules/employee-setup.ts` (line 263) returns the created designation — confirm the return shape; if it returns the id, use it directly, otherwise read it from the refetched form options by name.
- Expose `{ designationName, designationError, isCreatingDesignation, onDesignationNameChange, onAddDesignation }` through the hook; the create/edit routes (`employee.create.tsx`, `employee.$employeeId.edit.tsx`) pass it into `EmployeeForm` as a new optional prop, mirroring the existing `customFieldManager` prop pattern.

## 2. Remove the Employee Setup page

- Delete `apps/web/src/routes/institution-settings._index.tsx`. Also delete `institution-settings.tsx` (the layout parent) if nothing else lives under that path, so no orphan route remains.
- Remove the "Employee Setup" sidebar item in `apps/web/src/components/app-sidebar.tsx` (lines 60–62) and the dashboard link in `apps/web/src/routes/dashboard.tsx` (lines 65–67).
- Update the empty-state copy in `apps/web/src/routes/employee._index.tsx` line 344 ("Start by creating a designation in Employee Setup…") to reference creating one from the employee form.
- Note: reorder/archive UI disappears with the page (backend `reorderDesignations`/`archiveDesignation` procedures remain, unused by the web app — per your choice, create+select only).

## 3. E2E updates (required by repo guidelines)

- `e2e/tests/employee-setup.regression.spec.ts` (631 lines) is the big rework: designation create/validation tests move to the inline dialog flow (open employee create page → click + → create); ordering/archival UI tests are removed with the page; the access-control check that `goto("/institution-settings")` is guarded (line 302) becomes "route no longer exists / redirects"; keyboard/a11y tests move to the dialog.
- `e2e/tests/employee-setup.smoke.spec.ts`: drop the nav-link assertion and the `goToInstitutionSettings` flow; assert designation creation via the inline dialog on the employee create page.
- `e2e/tests/employee.regression.spec.ts` line 187 (creates a second designation via the settings page) switches to the inline dialog.
- `e2e/tests/institution.regression.spec.ts` lines 271–294: drop/adjust the `/institution-settings` navigation assertions.
- `e2e/src/helpers.ts`: repurpose `goToInstitutionSettings`/`createDesignation` UI helpers to drive the inline dialog.

## 4. Verification

- `bun run check` and `bun run lint`.
- `bun run test` for the Playwright suite (uses the disposable-account env vars).
- Manual pass: employee create form → + button → dialog → create → auto-selected; edit form same; empty-institution case (no designations) unlocks submit after inline create.
